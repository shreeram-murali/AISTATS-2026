from setuptools import setup, find_packages

setup(
    name="reml",
    version="0.1.0",
    description="Nadaraya-Watson Classifiers",
    author="Anonymous Submitter",
    author_email="anon@example.com",
    packages=find_packages(['reml'])
)